# Creative, Sophisticated & Zobra Tool for OS LAB

## Setup

You've Two Options:
- copy the script to `/bin`
```bash
cp ./WAYLAND_C_BIN /bin
```
- to `/usr/bin` or `$HOME/.local/bin`
```bash
cp ./WAYLAND_C_BIN /usr/bin
```

## Get Help
```bash
WAYLAND_C_BIN -h
```
